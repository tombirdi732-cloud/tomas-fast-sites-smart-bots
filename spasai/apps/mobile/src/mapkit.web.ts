/**
 * Веб-версия модуля карт.
 *
 * MapKit — нативный SDK, на вебе его нет. Метро выбирает этот файл вместо
 * `mapkit.ts` по расширению `.web`, поэтому нативный пакет в веб-бандл вообще
 * не попадает: без такого разделения сборка падает на native-only импортах.
 *
 * Экран карты в ответ на `null` рисует схему — на вебе она и остаётся.
 */

export const MAPKIT_KEY: string | null = null;

export function loadYamap(): null {
  return null;
}
