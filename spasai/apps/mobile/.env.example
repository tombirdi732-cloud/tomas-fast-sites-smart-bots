# Скопируйте в .env — файл в git не попадает.
# Переменные с префиксом EXPO_PUBLIC_ Metro подставляет в бандл на сборке.

# Ключ MapKit Mobile SDK из кабинета Яндекса.
# Без него экран карты показывает схему вместо настоящей карты.
EXPO_PUBLIC_MAPKIT_API_KEY=
